[project]
name = "tp0-pdi"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "nost17", email = "erick.personal16@outlook.com" }
]
requires-python = ">=3.12"
dependencies = [
    "matplotlib>=3.11.1",
    "numpy>=2.5.2",
    "pillow>=12.3.0",
]

[project.scripts]
tp0-pdi = "tp0_pdi:main"

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"
